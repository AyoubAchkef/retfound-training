"""
RETFound Tests Package
======================

Test suite for the RETFound training system.
"""

import sys
from pathlib import Path

# Add project root to path for testing
project_root = Path(__file__).parent.parent
sys.path.insert(0, str(project_root))
